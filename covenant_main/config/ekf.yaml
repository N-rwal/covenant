ekf_filter_node:
  ros__parameters:
    frequency: 20
    two_d_mode: true

    odom_frame: odom
    base_link_frame: base_link
    world_frame: odom

    # use FC pose as odom
    odom0: /ap/pose/filtered
    odom0_config: [true,  true,  false,
                   false, false, true,
                   false, false, false,
                   false, false, false,
                   false, false, false]
